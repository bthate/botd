if [ ! -f "/etc/systemd/system/botd.service" ] ; then 
    echo "writing /etc/systemd/system/botd.service"
    cat > /etc/systemd/system/botd.service <<-EOM
[Unit]
Description=botd

[Service]
ExecStart=/usr/local/bin/botd
After=network-online.target
Wants=network-online.target

[Install]
WantedBy=multi-user.target
EOM
fi
boted bot.irc.Cfg server "$1"
boted bot.irc.Cfg channel "$2"
boted bot.irc.Cfg nick "$3"
boted lo.krn.Cfg modules "$4"
boted lo.krn.Cfg owner "$5"
botctl meet "$5" --owner root@shell
bothup

