#!/usr/bin/python3 

txt = """# BOTD - python3 IRC channel daemon\n#\n#\n"""

import os, sys ; sys.path.insert(0, os.getcwd())

import json
import time
import bl.table

from bl.krn import Kernel
k = Kernel()
k.walk("bl")
k.walk("botd")
print(txt)
print("classes = %s" % bl.table.classes)
print("names = %s" % bl.table.names)
print("modules = %s" % bl.table.modules)
sys.exit()