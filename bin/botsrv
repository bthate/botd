#!/usr/bin/python3

from bot.hdl import Handler, os
from bot.trm import execute

def main():
    import bot.obj
    bot.obj.wd = "/var/lib/bot"
    h = Handler()
    h.start()
    h.scan("irc")
    h.init("irc,udp,rss")
    h.wait()
     
execute(main)
os._exit(0)
