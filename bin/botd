#!/usr/bin/python3

from bot.hdl import Handler, os
from bot.trm import execute

def main():
    import bot.obj
    bot.obj.wd = "/var/lib/botd"
    h = Handler()
    h.start()
    h.walk("bot")
    h.init("bot.irc,bot.udp,bot.rss")
    h.wait()
     
execute(main)
os._exit(0)
